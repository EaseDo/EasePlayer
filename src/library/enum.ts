/**
 * 
 */
export enum EasePlayerType {
    VIDEO, LIVE,
}

export enum EasePlayerTheme {
    AUTO    = 'auto',
    PC      = 'pc',
    MOBILE  = 'mobile'
}

export enum EasePlayerStatus {
    INIT,
    LOADING,
    PLAYING,
    PAUSED,
    ERROR
}